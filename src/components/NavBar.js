import React from "react";
import {
  AppBar,
  Toolbar,
  IconButton,
  Typography,
  Button,
} from "@material-ui/core";

let NavBar = () => (
  <AppBar position="static">
    <Typography variant="h1">Sporclone</Typography>
  </AppBar>
);
